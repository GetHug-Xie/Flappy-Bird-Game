import bagel.Image;

/**
 * PlasticPipeSet class extends from abstract class PipeSet.
 */
public class PlasticPipeSet extends PipeSet {
    /**
     * Inherits constructor from its abstract class.
     */
    public PlasticPipeSet(Image pipeImage, boolean isLevelUp) {
        super(pipeImage, isLevelUp);
    }

    /**
     * Inherits the update method from its abstract class.
     */
    public void update(int timeScale) {
        super.update(timeScale);
    }
}
